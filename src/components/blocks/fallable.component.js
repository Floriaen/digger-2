import { Component } from '../../core/component.js';

/**
 * FallableComponent
 *
 * Marks a block as affected by gravity.
 * Blocks with this component will fall when unsupported.
 */
export class FallableComponent extends Component {
  constructor() {
    super();
  }
}
